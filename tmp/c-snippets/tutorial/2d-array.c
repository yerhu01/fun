#include <stdio.h>

int **proper_2d(int) {
    int (*ret)[col] = malloc(sizeof(int[row][col]));
    
    return ret;
}
// array decay to pointer as function parameter => sizeof() gives ptr size

int main()
{
    //SCAN for array size

    
    return 0;
}
